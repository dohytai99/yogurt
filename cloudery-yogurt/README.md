# Cloudery Yogurt Landing

Static landing page (HTML/CSS/JS only).

## Run

- Open `cloudery-yogurt/index.html` in your browser.
- (Optional) Serve locally:

```bash
cd cloudery-yogurt
python3 -m http.server 5173
```

